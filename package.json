{
  "name": "nexio-v6",
  "version": "6.4.0",
  "private": true,
  "type": "module",
  "description": "Monitored Binance Futures and Alpha engine with fast and steady momentum coverage",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --test",
    "smoke": "node scripts/binance-smoke.js"
  }
}
