{
  "name": "nexio-v6",
  "version": "6.6.0",
  "private": true,
  "type": "module",
  "description": "Monitored Futures and Alpha engine with adaptive BTC trend/retest gating and isolated macro reminders",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --test",
    "smoke": "node scripts/binance-smoke.js",
    "audit:futures": "node scripts/futures-live-audit.js"
  }
}
