{
  "name": "nexio-v6",
  "version": "6.1.0",
  "private": true,
  "type": "module",
  "description": "Closed-bar Binance USD-M and Alpha signal engine with on-chain rug screening",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --test",
    "smoke": "node scripts/binance-smoke.js"
  }
}
