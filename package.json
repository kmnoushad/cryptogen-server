{
  "name": "nexio-v6",
  "version": "6.5.0",
  "private": true,
  "type": "module",
  "description": "Monitored Futures and Alpha engine with isolated Finnhub macro-event reminders",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --test",
    "smoke": "node scripts/binance-smoke.js"
  }
}
